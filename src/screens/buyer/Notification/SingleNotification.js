import React, {Component} from 'react';
import {Text, View} from 'react-native';

export default class SingleNotification extends Component {
  render() {
    return (
      <View>
        <Text> Single Notification </Text>
      </View>
    );
  }
}
